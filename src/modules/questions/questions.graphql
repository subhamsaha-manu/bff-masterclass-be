extend type Query{
    questions(subtopicId: ID!):[Question]!
    getQuestion(questionId: ID!):Question!
}

type Question {
    uuid: ID!
    title: String!
    choices: [Choice!]!
    weight: Int!
}

type Choice {
    uuid: ID!
    title: String!
    isCorrect: Boolean!
}
